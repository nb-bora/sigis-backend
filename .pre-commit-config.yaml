# Hooks locaux optionnels : pip install pre-commit && pre-commit install
# Alignés sur le job CI (Ruff).

repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.9.6
    hooks:
      - id: ruff
        args: [--fix]
      - id: ruff-format
